services:
  client:
    build:
      context: ./client
    ports:
      - "81:8080"
    environment:
      SERVER_HOST: server
      
  server:
    build:
      context: ./server  
    ports:
      - "3001:3000" 
