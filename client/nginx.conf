server {
    location /ws/ {
        proxy_pass http://server:3000/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    listen 8080;

    location = / {
        root /usr/src/app;
        try_files /landingpage/landing.html =404;
    }

    location = /papers {
        root /usr/src/app/landingpage;
        try_files /papers.html =404;
    }

    location /papers/ {
        alias /usr/src/app/landingpage/papers/;
    }

    location /images/ {
        alias /usr/src/app/landingpage/images/;
    }

    location /skypirates {
        alias /usr/src/app/;
        try_files $uri $uri/ /index.html;
    }

    location /jetgame {
        proxy_pass http://jet-game:80/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /maizemodel {
        proxy_pass http://maize-model:80/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 443 ssl;
    ssl_certificate /usr/src/app/certs/fullchain.pem;
    ssl_certificate_key /usr/src/app/certs/privkey.pem;
        location /ws/ {
            proxy_pass http://server:3000/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

    location = / {
        root /usr/src/app;
        try_files /landingpage/landing.html =404;
    }

    location = /papers {
        root /usr/src/app/landingpage;
        try_files /papers.html =404;
    }

    location /papers/ {
        alias /usr/src/app/landingpage/papers/;
    }

    location /images/ {
        alias /usr/src/app/landingpage/images/;
    }

    location /skypirates {
        alias /usr/src/app/;
        try_files $uri $uri/ /index.html;
    }

    location /jetgame {
        proxy_pass http://jet-game:80/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /maizemodel {
        proxy_pass http://maize-model:80/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
